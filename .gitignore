node_modules/
dist/
coverage/
*.log
.env
.env.*.local
# Every real env file except the checked-in templates — catches
# .env.production, .env.staging, etc. without an allowlist gap.
.env.*
!.env.example
!.env.production.example

# data volumes / local artifacts
/data/
openapi.json

# editor
.vscode/
.idea/

# prisma
prisma/*.db

# archives — backup output (scripts/backup.sh writes .tar.gz/.dump.gpg),
# handoff bundles, etc. Nothing this size belongs in git history.
*.zip
*.tar
*.tar.gz
*.tgz
*.tar.bz2
*.tbz2
*.tar.xz
*.txz
*.gz
*.bz2
*.xz
*.7z
*.rar
